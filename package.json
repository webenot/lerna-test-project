{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "npx lerna run build",
    "tests": "npx lerna run test",
    "lint": "npx lerna run lint"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.6.2"
  }
}
